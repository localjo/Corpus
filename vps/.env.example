# Optional webhook for failure notifications
NOTIFY_WEBHOOK_URL=

# Git defaults
DEFAULT_BRANCH=main
COMMIT_MESSAGE_PREFIX=sync

# Optional git identity for cron commits
GIT_AUTHOR_NAME=Corpus Bot
GIT_AUTHOR_EMAIL=corpus-bot@example.com
